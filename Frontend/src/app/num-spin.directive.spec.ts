import { NumSpinDirective } from './num-spin.directive';

describe('NumSpinDirective', () => {
  it('should create an instance', () => {
    const directive = new NumSpinDirective();
    expect(directive).toBeTruthy();
  });
});
